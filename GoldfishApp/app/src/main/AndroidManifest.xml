<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.goldfish">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivityHomeMenu"
            />
        <activity
            android:name=".Settings"
            android:parentActivityName=".MainActivityHomeMenu"/>
        <activity
            android:name=".SoundSettings"
            android:exported="true"
            android:parentActivityName=".Settings" />
        <activity
            android:name=".DifficultyLevel"
            android:parentActivityName=".Settings" />
        <activity
            android:name=".MultiplayerSetting"
            android:parentActivityName=".Settings" />
        <activity
            android:name=".GamePlay"
            android:parentActivityName=".MainActivityHomeMenu" />
        <activity android:name=".WelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>